# VRC-RunicChat
A basic script meant to translate text into runs and slowly reveal the true text

Currently this sends the runes to VRChat waits a second then sends the plaintext message
also in this repository is RunicTranslate which was a proof of concept for runic translation I decided to keep
